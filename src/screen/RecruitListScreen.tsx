import {Text, View} from 'react-native';
import common from '@styles/common';

function RecruitListScreen() {
  return (
    <View>
      <Text style={common.text_m}>구인 목록 화면</Text>
    </View>
  );
}

export default RecruitListScreen;
